package com.grapecity.leyser.badge;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class BadgeSpec extends NativeBadgeSpec {
  BadgeSpec(ReactApplicationContext context) {
    super(context);
  }
}
